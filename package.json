{
  "name": "if-professional-astrea",
  "version": "0.1.0",
  "private": true,
  "description": "If Professional ASTREA — ASTREA Theme と ASTREA Core のリポジトリ。開発環境はWSL2 + Docker + @wordpress/env（Decision 020）。",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:cli": "wp-env run cli --",
    "env:logs": "wp-env logs",
    "smoke-test": "bash tools/ci/smoke-test.sh"
  },
  "devDependencies": {
    "@wordpress/env": "^11.13.0"
  },
  "engines": {
    "node": ">=18.12.0",
    "npm": ">=8.19.2"
  }
}
